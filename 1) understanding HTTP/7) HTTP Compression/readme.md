# HTTP Compression:
- This enables the Server to Send the response in compressed format to the Client thus reducing the size, which reduces the time it takes to load in the client side.
- Then in client side the compressed file/ resource is decompressed and rendered